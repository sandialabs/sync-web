name: Router Service

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "main" ]

concurrency:
  group: "router"
  cancel-in-progress: true

jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Login to Github registry
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Build versioned Docker image
        run: |
          version=$( cat ./services/router/version.txt )
          docker build ./services/router --tag ghcr.io/${{ github.repository }}/router:$version
      - name: Push versioned Docker image
        if: github.ref == 'refs/heads/main'
        run: |
          version=$( cat ./services/router/version.txt )
          docker push ghcr.io/${{ github.repository }}/router:$version
      - name: Retag and push latest Docker image
        if: github.ref == 'refs/heads/main'
        run: |
          version=$( cat ./services/router/version.txt )
          docker tag ghcr.io/${{ github.repository }}/router:$version \
                     ghcr.io/${{ github.repository }}/router:latest
          docker push ghcr.io/${{ github.repository }}/router:latest
