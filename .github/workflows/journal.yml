name: Journal & Compose

on:
  workflow_dispatch:
  push:
    branches: ["**"]
    paths: ["journal/**", "records/**", "deploy/**", "VERSION", ".github/workflows/journal.yml"]
  pull_request:
    branches: ["main"]
    paths: ["journal/**", "records/**", "deploy/**", ".github/workflows/journal.yml"]

concurrency:
  group: "journal-compose-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      journal: ${{ steps.filter.outputs.journal }}
      version: ${{ steps.filter.outputs.version }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            journal:
              - 'journal/**'
            version:
              - 'VERSION'

  test-journal:
    needs: changes
    if: (needs.changes.outputs.journal == 'true' || needs.changes.outputs.version == 'true') && !(github.ref == 'refs/heads/main' && needs.changes.outputs.version == 'true')
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Install dependencies
        run: sudo apt update && sudo apt install -y llvm-dev libclang-dev clang
      - uses: dtolnay/rust-toolchain@stable
      - uses: Swatinem/rust-cache@v2
        with:
          workspaces: journal
      - name: Build
        working-directory: journal
        run: cargo build
      - name: Test
        working-directory: journal
        run: cargo test

  build-journal-sdk:
    needs: [changes, test-journal]
    if: (needs.changes.outputs.journal == 'true' || needs.changes.outputs.version == 'true') && needs.test-journal.result == 'success'
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - uses: actions/checkout@v4
      - name: Login to Github registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: docker/setup-qemu-action@v3
      - uses: docker/setup-buildx-action@v3
      - name: Build journal-sdk pull request validation image
        if: github.event_name == 'pull_request'
        uses: docker/build-push-action@v6
        with:
          context: journal
          target: runtime
          platforms: linux/amd64
          push: false
          tags: ghcr.io/${{ github.repository }}/journal-sdk:pr-validation
          labels: org.opencontainers.image.source=https://github.com/${{ github.repository }}
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}/journal-sdk:cache
      - name: Build and push SHA-tagged journal-sdk validation image
        if: github.event_name != 'pull_request' && github.ref != 'refs/heads/main'
        uses: docker/build-push-action@v6
        with:
          context: journal
          target: runtime
          platforms: linux/amd64
          push: true
          tags: ghcr.io/${{ github.repository }}/journal-sdk:sha-${{ github.sha }}
          labels: org.opencontainers.image.source=https://github.com/${{ github.repository }}
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}/journal-sdk:cache
          cache-to: type=registry,ref=ghcr.io/${{ github.repository }}/journal-sdk:cache,mode=max
      - name: Build and push journal-sdk for direct main journal changes
        if: github.ref == 'refs/heads/main' && needs.changes.outputs.version != 'true'
        uses: docker/build-push-action@v6
        with:
          context: journal
          target: runtime
          platforms: linux/amd64,linux/arm64
          push: true
          tags: ghcr.io/${{ github.repository }}/journal-sdk:latest
          labels: org.opencontainers.image.source=https://github.com/${{ github.repository }}
          cache-from: type=registry,ref=ghcr.io/${{ github.repository }}/journal-sdk:cache
          cache-to: type=registry,ref=ghcr.io/${{ github.repository }}/journal-sdk:cache,mode=max

  compose-smoke:
    needs: [changes, test-journal, build-journal-sdk]
    if: always() && github.ref != 'refs/heads/main' && !cancelled() && !failure()
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Run compose smoke test
        run: |
          SECRET=password \
          PORT=8192 \
          tests/api/local-compose.sh smoke

  tag-version:
    needs: [changes, build-journal-sdk]
    if: always() && github.ref == 'refs/heads/main' && needs.changes.outputs.version == 'true' && (needs.build-journal-sdk.result == 'success' || needs.build-journal-sdk.result == 'skipped')
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Login to Github registry
        if: github.event_name != 'pull_request'
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - uses: actions/checkout@v4
      - uses: docker/setup-buildx-action@v3
      - name: Promote validated image
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}
        run: .github/scripts/promote-image.sh journal-sdk
