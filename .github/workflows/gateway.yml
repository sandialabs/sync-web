name: Gateway Service

on:
  push:
    branches: [ "**" ]
  pull_request:
    branches: [ "main" ]

concurrency:
  group: "gateway"
  cancel-in-progress: true

jobs:
  run:
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    steps:
      - name: Login to Github registry
        if: github.ref == 'refs/heads/main'
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install gateway dependencies
        working-directory: ./services/gateway
        run: npm ci
      - name: TypeScript build check
        working-directory: ./services/gateway
        run: npm run build
      - name: Build versioned Docker image
        run: |
          version=$( cat ./services/gateway/version.txt )
          docker build ./services/gateway --tag ghcr.io/${{ github.repository }}/gateway:$version
      - name: Push versioned Docker image
        if: github.ref == 'refs/heads/main'
        run: |
          version=$( cat ./services/gateway/version.txt )
          docker push ghcr.io/${{ github.repository }}/gateway:$version
      - name: Retag and push latest Docker image
        if: github.ref == 'refs/heads/main'
        run: |
          version=$( cat ./services/gateway/version.txt )
          docker tag ghcr.io/${{ github.repository }}/gateway:$version \
                     ghcr.io/${{ github.repository }}/gateway:latest
          docker push ghcr.io/${{ github.repository }}/gateway:latest
