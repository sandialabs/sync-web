name: Records

on:
  workflow_dispatch:
  push:
    branches: ["**"]
    paths: ["records/**", "journal/**", "deploy/compose/general/**", ".github/workflows/records.yml"]
  pull_request:
    branches: ["main"]
    paths: ["records/**", "journal/**", "deploy/compose/general/**", ".github/workflows/records.yml"]

concurrency:
  group: "records-${{ github.ref }}"
  cancel-in-progress: true

jobs:
  changes:
    runs-on: ubuntu-latest
    outputs:
      core: ${{ steps.filter.outputs.core }}
    steps:
      - uses: actions/checkout@v4
      - uses: dorny/paths-filter@v3
        id: filter
        with:
          filters: |
            core:
              - 'records/**'
              - 'journal/**'

  test:
    needs: changes
    if: needs.changes.outputs.core == 'true'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
      - name: Install dependencies
        run: |
          sudo apt update
          sudo apt install -y llvm-dev libclang-dev clang
      - name: Install rust
        uses: dtolnay/rust-toolchain@stable
      - name: Cache Rust build artifacts
        uses: Swatinem/rust-cache@v2
        with:
          workspaces: |
            journal
            records/tests
      - name: Build records test runner
        run: |
          CARGO_TARGET_DIR=journal/target cargo test --manifest-path records/tests/Cargo.toml
          CARGO_TARGET_DIR=journal/target cargo build --release --manifest-path records/tests/Cargo.toml
      - name: Run records tests
        run: journal/target/release/records-test --suite records/tests/suite.toml --jobs 2
