{
  "add": {
    "description": "Add two and two together",
    "code": "(*eval* \"mysecret\" (+ 2 2))"
  },
  "fibinacci": {
    "description": "Compute a fibinacci sequence",
    "code": "(*eval* \"mysecret\" (let loop ((a 0) (b 1) (n 10) (acc ())) (if (= n 0) (reverse acc) (loop b (+ a b) (- n 1) (cons a acc)))))"
  },
  "merkle": {
    "description": "Compute the digest of four short byte vectors",
    "code": "(*eval* \"mysecret\" (sync-digest (sync-cons (sync-cons #u(0) #u(1)) (sync-cons #u(2) #u(3)))))"
  },
  "update standard": {
    "description": "Update the logic of the standard class",
    "code": "(*call* 'admin-password (lambda (root) (define standard-cls  '(define-class (standard) ...)) ((root 'set!) '(root class standard) standard-cls) ((root 'set!) '(root object standard) (((eval (caddr standard-cls)) #f standard-cls)))))))"
  },
  "update ledger": {
    "description": "Update the logic of the tree class and in the ledger object",
    "code": "(*call* 'admin-password (lambda (root) (define ledger-cls '(define-class (ledger) ...)) (let* ((standard-node ((root 'get) '(root object standard))) (standard ((eval (byte-vector->expression (sync-car standard-node))) standard-node)) (ledger-node ((root 'get) '(root object ledger)))) ((root 'set!) '(root class ledger) ledger-cls) ((root 'set!) '(root object ledger) (sync-cons (sync-car (((standard 'make) ledger-cls #f))) (sync-cdr ledger-node))))))))"
  }
}
