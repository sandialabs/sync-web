{
  "size": {
    "description": "Returns the current size (number of entries) of the permanent chain. This represents how many state snapshots have been committed.",
    "template": "((function size))",
    "example": "((function size))",
    "permission": "any"
  },
  "trace": {
    "description": "Returns a serialized traced object for a committed path at a specific chain index. Used by bridges and remote synchronization helpers.",
    "template": "((function trace) (arguments ((index <index>) (path <path>))))",
    "example": "((function trace) (arguments ((index 0) (path (*state* key)))))",
    "permission": "any"
  },
  "trace-batch": {
    "description": "Returns one compact serialized union proof for up to 1,024 authorized paths sharing one history anchor. Heterogeneous anchors require separate calls or retrieve-batch grouping.",
    "template": "((function trace-batch) (arguments ((index <index>) (paths (<path> ...)))))",
    "example": "((function trace-batch) (arguments ((index 0) (paths ((*state* alice one) (*state* alice two))))))",
    "permission": "any"
  },
  "info": {
    "description": "Returns public config info about this journal, including the public key and window size. Used by bridges to identify and verify this journal.",
    "template": "((function info))",
    "example": "((function info))",
    "permission": "any"
  },
  "put!": {
    "description": "Stages inert content or, with object? true, one uninitialized Standard class shell. Object expectations compare code digest and ignore state.",
    "template": "((function put!) (arguments ((path <path>) (value <value-or-class>) (object? <object?>) (expected <old-value-or-class>) (expression? <expression?>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function put!) (arguments ((path (*state* alice counter)) (value (define-class (counter) (define-method (value self) 0))) (object? #t) (expression? #t))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "use!": {
    "description": "Exercises staged inert content or an opaque resource method. `read-only? #f` persists a changed successor atomically; `#t` returns the same validated result and always discards it. Blank read-only inert use is the staged read operation.",
    "template": "((function use!) (arguments ((path <path>) (method <method>) (arguments (<argument> ...)) (read-only? <boolean>) (expression? <expression?>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function use!) (arguments ((path (*state* alice counter)) (method increment!) (arguments (1)) (expression? #t))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "copy!": {
    "description": "Copies exact raw staged content from source to path in one terminal Journal. Optional expected content makes the target write conditional; mismatch returns #f without mutation.",
    "template": "((function copy!) (arguments ((source <source-path>) (path <target-path>) (expected <target-old-value>) (expression? <expression?>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function copy!) (arguments ((source (*state* alice documents source)) (path (*state* alice documents copy)) (expected #f) (expression? #t))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "run!": {
    "description": "Runs one staged Scheme orchestration procedure with the authenticated Journal capability and explicit argument list.",
    "template": "((function run!) (arguments ((path <staged-path>) (arguments (<argument> ...)))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function run!) (arguments ((path (*state* alice programs example)) (arguments (1 2)))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "truncate!": {
    "description": "Destructive and irreversible: releases locally available Ledger history through the inclusive index. Self-local configured Interface administrators/root only; grants and remote routes cannot confer it.",
    "template": "((function truncate!) (arguments ((index <inclusive-cutoff>))) (authentication ((identity (*state* <admin>)) (credentials <secret>))))",
    "example": "((function truncate!) (arguments ((index -1))) (authentication ((identity (*state* admin)) (credentials \"mysecret\"))))",
    "permission": "admin"
  },
  "pin!": {
    "description": "Pins data at the specified path to prevent it from being pruned. The path must include a chain index as the first element. Pinned data is preserved in the permanent chain.",
    "template": "((function pin!) (arguments ((path <path>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function pin!) (arguments ((path (-1 *state* alice important-data)))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "pin-batch!": {
    "description": "Atomically pins up to 1,024 complete origin-relative committed paths. Remote proof groups are fetched and verified before the one local retention mutation; any failed member rolls back all retention changes.",
    "template": "((function pin-batch!) (arguments ((paths (<committed-path> ...)))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function pin-batch!) (arguments ((paths ((-1 *state* alice important-data) (-1 peer-a -1 *state* bob shared))))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "unpin!": {
    "description": "Unpins data at the specified path, allowing it to be pruned. The path must include a chain index as the first element.",
    "template": "((function unpin!) (arguments ((path <path>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function unpin!) (arguments ((path (-1 *state* alice old-data)))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "unpin-batch!": {
    "description": "Atomically removes retained proof material for up to 1,024 complete origin-relative paths. Duplicate and overlapping cuts are idempotent; malformed members roll back the batch.",
    "template": "((function unpin-batch!) (arguments ((paths (<committed-path> ...)))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function unpin-batch!) (arguments ((paths ((-1 *state* alice old-data) (-1 peer-a -1 *state* bob old-data))))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "prune!": {
    "description": "Removes one retained committed leaf or directory from both temporary and permanent Ledger retention while preserving Stage and committed history identity. Configured local Interface administrators/root only.",
    "template": "((function prune!) (arguments ((path <committed-path>))) (authentication ((identity (*state* <admin>)) (credentials <secret>))))",
    "example": "((function prune!) (arguments ((path (-1 *state* alice old-data)))) (authentication ((identity (*state* admin)) (credentials \"mysecret\"))))",
    "permission": "admin"
  },
  "prune-batch!": {
    "description": "Atomically removes the union of up to 1,024 retained committed paths from both temporary and permanent Ledger retention. Configured local Interface administrators/root only.",
    "template": "((function prune-batch!) (arguments ((paths (<committed-path> ...)))) (authentication ((identity (*state* <admin>)) (credentials <secret>))))",
    "example": "((function prune-batch!) (arguments ((paths ((-1 *state* alice old-data) (-1 *state* alice archive))))) (authentication ((identity (*state* admin)) (credentials \"mysecret\"))))",
    "permission": "admin"
  },
  "config": {
    "description": "Returns the full config of the ledger, including both public and private settings.",
    "template": "((function config) (authentication ((identity (*state* <admin>)) (credentials <secret>))))",
    "example": "((function config) (authentication ((identity (*state* admin)) (credentials \"mysecret\"))))",
    "permission": "admin"
  },
  "bridge!": {
    "description": "Creates a reciprocal bridge and takes responsibility for initiating synchronization.",
    "template": "((function bridge!) (arguments ((name <name>) (interface <endpoint>) (remote-name <remote-name>))) (authentication ((identity (*state* <admin>)) (credentials <secret>))))",
    "example": "((function bridge!) (arguments ((name alice) (interface \"http://alice.example.com/api/v1/journal/interface\") (remote-name local-journal))) (authentication ((identity (*state* admin)) (credentials \"mysecret\"))))",
    "permission": "admin"
  },
  "update-config!": {
    "description": "Updates a ledger configuration path. Used for bridge acceptance and preapproval settings.",
    "template": "((function update-config!) (arguments ((path <path>) (value <value>))) (authentication ((identity (*state* <admin>)) (credentials <secret>))))",
    "example": "((function update-config!) (arguments ((path (public bridge-accept)) (value preapproved))) (authentication ((identity (*state* admin)) (credentials \"mysecret\"))))",
    "permission": "admin"
  },
  "*secret*": {
    "description": "Rotates the journal-wide Interface authentication secret and corresponding Interface public signing key. Configured local Interface administrators/root only.",
    "template": "((function *secret*) (arguments ((secret <new-interface-secret>))) (authentication ((identity (*state* <admin>)) (credentials <current-interface-secret>))))",
    "example": "((function *secret*) (arguments ((secret \"new-interface-secret\"))) (authentication ((identity (*state* admin)) (credentials \"current-interface-secret\"))))",
    "permission": "admin"
  },
  "put-batch!": {
    "description": "Atomically stages ordered inert/object replacements with parallel object flags and one expectation snapshot.",
    "template": "((function put-batch!) (arguments ((paths (<path> ...)) (values (<value-or-class> ...)) (object? (<boolean> ...)) (expected (<old-value-or-class> ...)) (expression? <expression?>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function put-batch!) (arguments ((paths ((*state* alice a) (*state* alice b))) (values (1 2)) (object? (#f #f)) (expression? #t))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "use-batch!": {
    "description": "Exercises ordered staged resources with one scalar `read-only?`. Mutating mode retains ordered successor visibility and full rollback; read-only mode returns ordered results and discards all successors.",
    "template": "((function use-batch!) (arguments ((paths (<path> ...)) (methods (<method> ...)) (arguments ((<argument> ...) ...)) (read-only? <boolean>) (expression? <expression?>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function use-batch!) (arguments ((paths ((*state* alice counter) (*state* alice counter))) (methods (increment! value)) (arguments ((1) ())) (expression? #t))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "delete-bridge!": {
    "description": "Deletes a local reciprocal bridge and its alias-scoped authorization state. Configured local Interface administrators/root only.",
    "template": "((function delete-bridge!) (arguments ((name <bridge-name>))) (authentication ((identity (*state* <admin>)) (credentials <secret>))))",
    "example": "((function delete-bridge!) (arguments ((name peer-a))) (authentication ((identity (*state* admin)) (credentials \"mysecret\"))))",
    "permission": "admin"
  },
  "authorizations": {
    "description": "Lists exact authorization rules owned by a local (*state* USER) namespace. Owners may list their own rules; Interface administrators/root may manage local namespaces.",
    "template": "((function authorizations) (arguments ((user (*state* <owner>)))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function authorizations) (arguments ((user (*state* alice)))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))",
    "permission": "user"
  },
  "authorize!": {
    "description": "Adds an exact owner-local rule. Remote principals require terminal authentication key-index; Resolve has a separate document-history range. Exact local/public rules omit key-index.",
    "template": "((function authorize!) (arguments ((user (*state* <owner>)) (rule <exact-rule>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function authorize!) (arguments ((user (*state* alice)) (rule ((principal (peer-a *state* bob)) (key-index (-32 -1)) (path (docs)) (put! #f) (use! ((read-only? #t))) (run! #f) (retrieve (0 -1)))))) (authentication ((identity (*state* alice)) (credentials \"mysecret\")))))",
    "permission": "user"
  },
  "deauthorize!": {
    "description": "Deletes an owner-local authorization rule using the complete exact stored shape, including key-index and Resolve range.",
    "template": "((function deauthorize!) (arguments ((user (*state* <owner>)) (rule <exact-rule>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "example": "((function deauthorize!) (arguments ((user (*state* alice)) (rule ((principal (peer-a *state* bob)) (key-index (-32 -1)) (path (docs)) (put! #f) (use! ((read-only? #t))) (run! #f) (retrieve (0 -1)))))) (authentication ((identity (*state* alice)) (credentials \"mysecret\")))))",
    "permission": "user"
  },
  "*admins-get*": {
    "description": "Returns null when no administrators are configured or a username-keyed map of exact local Interface administrator principals. Interface administrators/root only.",
    "template": "((function *admins-get*) (authentication ((identity (*state* <admin>)) (credentials <secret>))))",
    "example": "((function *admins-get*) (authentication ((identity (*state* admin)) (credentials \"mysecret\"))))",
    "permission": "admin"
  },
  "*admins-set*": {
    "description": "Atomically replaces configured local Interface administrator principals. Every exact username key must match its (*state* USER) value. Interface administrators/root only.",
    "template": "((function *admins-set*) (arguments ((admins ((<user> (*state* <user>)) ...)))) (authentication ((identity (*state* <admin>)) (credentials <secret>))))",
    "example": "((function *admins-set*) (arguments ((admins ((admin (*state* admin)) (alice (*state* alice)))))) (authentication ((identity (*state* admin)) (credentials \"mysecret\"))))",
    "permission": "admin"
  },
  "*window-set*": {
    "description": "Sets the positive ledger retention window. Interface administrators/root only; decreasing may prune unpinned history.",
    "template": "((function *window-set*) (arguments ((value <positive-integer>))) (authentication ((identity (*state* <admin>)) (credentials <secret>))))",
    "example": "((function *window-set*) (arguments ((value 128))) (authentication ((identity (*state* admin)) (credentials \"mysecret\"))))",
    "permission": "admin"
  },
  "*eval*": {
    "description": "Evaluates an arbitrary Scheme expression with full access to the journal internals. Extremely powerful and dangerous - use with caution.",
    "template": "(*eval* <admin-secret> <expression>)",
    "example": "(*eval* \"admin-secret\" (+ 1 2 3))",
    "permission": "root"
  },
  "*call*": {
    "description": "Calls a function with the root object as an argument. The function receives direct access to the root data structure.",
    "template": "(*call* <admin-secret> <function>)",
    "example": "(*call* \"admin-secret\" (lambda (root) ((root 'get) '(root object ledger))))",
    "permission": "root"
  },
  "*step*": {
    "description": "Executes a full step cycle: by default it refreshes configured bridges, commits the current staged state, and returns the new chain size. An optional query can drive a custom installed step handler operation.",
    "template": "(*step* <admin-secret> [query])",
    "example": "(*step* \"admin-secret\")",
    "permission": "root"
  },
  "*set-secret*": {
    "description": "Changes the admin secret used for root-level operations. Requires the current admin secret for authentication.",
    "template": "(*set-secret* <current-secret> <new-secret>)",
    "example": "(*set-secret* \"old-secret\" \"new-secret\")",
    "permission": "root"
  },
  "*set-step*": {
    "description": "Sets the step handler function that is called during *step* operations. The function receives the root object, admin secret, and optional step query.",
    "template": "(*set-step* <admin-secret> <step-function>)",
    "example": "(*set-step* \"admin-secret\" (lambda (root secret query) ...)",
    "permission": "root"
  },
  "*set-query*": {
    "description": "Sets the query handler function that processes all incoming queries. The function receives the root object and query data.",
    "template": "(*set-query* <admin-secret> <query-function>)",
    "example": "(*set-query* \"admin-secret\" (lambda (root query) ...)",
    "permission": "root"
  },
  "retrieve": {
    "description": "Retrieves authenticated committed content or a deterministic method result at a selected history point. Verification is local and no successor persists.",
    "template": "((function retrieve) (arguments ((path <committed-path>) (method <method>) (arguments (<argument> ...)) (pinned? <pinned?>) (proof? <proof?>) (index? <index?>) (expression? <expression?>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "permission": "user",
    "example": "((function retrieve) (arguments ((path (-1 *state* alice key)) (pinned? #t) (proof? #t) (expression? #t))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))"
  },
  "retrieve-batch": {
    "description": "Retrieves ordered committed results with grouped verified proofs and no successor persistence.",
    "template": "((function retrieve-batch) (arguments ((paths (<committed-path> ...)) (methods (<method> ...)) (arguments ((<argument> ...) ...)) (pinned? <pinned?>) (index? <index?>) (expression? <expression?>))) (authentication ((identity (*state* <user>)) (credentials <secret>))))",
    "permission": "user",
    "example": "((function retrieve-batch) (arguments ((paths ((-1 *state* alice key))) (pinned? #t) (expression? #t))) (authentication ((identity (*state* alice)) (credentials \"mysecret\"))))"
  }
}
