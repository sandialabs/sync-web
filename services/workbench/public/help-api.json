{
  "size": {
    "description": "Returns the current size (number of entries) of the permanent chain. This represents how many state snapshots have been committed.",
    "template": "((function size))",
    "example": "((function size))",
    "permission": "any"
  },
  "synchronize": {
    "description": "Returns a serialized proof of the chain state up to the specified index. Used for bridge synchronization to verify chain integrity and fetch missing data.",
    "template": "((function synchronize) (arguments ((index <index>))))",
    "example": "((function synchronize) (arguments ((index 0))))",
    "permission": "any"
  },
  "resolve": {
    "description": "Returns committed/indexed content for a specific path. Optional pinned? and proof? flags control whether metadata and serialized proof material are included.",
    "template": "((function resolve) (arguments ((path <path>) (pinned? <pinned?>) (proof? <proof?>))) (authentication <secret>))",
    "example": "((function resolve) (arguments ((path (-1 (*state* key))) (pinned? #t) (proof? #f))) (authentication \"mysecret\"))",
    "permission": "user"
  },
  "trace": {
    "description": "Returns a serialized traced object for a committed path at a specific chain index. Used by bridges and remote synchronization helpers.",
    "template": "((function trace) (arguments ((index <index>) (path <path>))))",
    "example": "((function trace) (arguments ((index 0) (path (-1 (*state* key))))))",
    "permission": "any"
  },
  "info": {
    "description": "Returns public config info about this journal, including the public key and window size. Used by bridges to identify and verify this journal.",
    "template": "((function info))",
    "example": "((function info))",
    "permission": "any"
  },
  "get": {
    "description": "Retrieves data from the current staging area only. Use resolve for committed/indexed reads.",
    "template": "((function get) (arguments ((path <path>))) (authentication <secret>))",
    "example": "((function get) (arguments ((path ((*state* *time*))))) (authentication \"mysecret\"))",
    "permission": "user"
  },
  "set!": {
    "description": "Sets a value at the specified path in the staging area. The path must start with a list beginning with '*state*'. Changes are staged until the next *step* commits them.",
    "template": "((function set!) (arguments ((path <path>) (value <value>))) (authentication <secret>))",
    "example": "((function set!) (arguments ((path ((*state* mykey))) (value \"myvalue\"))) (authentication \"mysecret\"))",
    "permission": "user"
  },
  "pin!": {
    "description": "Pins data at the specified path to prevent it from being pruned. The path must include a chain index as the first element. Pinned data is preserved in the permanent chain.",
    "template": "((function pin!) (arguments ((path <path>))) (authentication <secret>))",
    "example": "((function pin!) (arguments ((path (-1 (*state* important-data))))) (authentication \"mysecret\"))",
    "permission": "user"
  },
  "unpin!": {
    "description": "Unpins data at the specified path, allowing it to be pruned. The path must include a chain index as the first element.",
    "template": "((function unpin!) (arguments ((path <path>))) (authentication <secret>))",
    "example": "((function unpin!) (arguments ((path (-1 (*state* old-data))))) (authentication \"mysecret\"))",
    "permission": "user"
  },
  "batch!": {
    "description": "Executes multiple general requests in order under one authenticated call. Each item in queries includes a function name and an optional arguments object.",
    "template": "((function batch!) (arguments ((queries (((function <function-1>) (arguments ((<key-1> <value-1>) ...))) ((function <function-2>)) ...)))) (authentication <secret>))",
    "example": "((function batch!) (arguments ((queries (((function get) (arguments ((path ((*state* *time*))))) ((function config)))))) (authentication \"mysecret\"))",
    "permission": "user"
  },
  "config": {
    "description": "Returns the full config of the ledger, including both public and private settings.",
    "template": "((function config) (authentication <secret>))",
    "example": "((function config) (authentication \"mysecret\"))",
    "permission": "user"
  },
  "bridge!": {
    "description": "Adds or updates a bridge with the given name and interface URL. The interface wires standard info, synchronize, and trace handlers for the remote peer.",
    "template": "((function bridge!) (arguments ((name <name>) (interface <endpoint>))) (authentication <secret>))",
    "example": "((function bridge!) (arguments ((name alice) (interface \"http://alice.example.com/interface\"))) (authentication \"mysecret\"))",
    "permission": "user"
  },
  "*secret*": {
    "description": "Changes the user authentication secret. Requires the current user secret for authentication.",
    "template": "((function *secret*) (arguments ((secret <new-secret>))) (authentication <old-secret>))",
    "example": "((function *secret*) (arguments ((secret \"new-user-secret\"))) (authentication \"old-user-secret\"))",
    "permission": "user"
  },
  "*eval*": {
    "description": "Evaluates an arbitrary Scheme expression with full access to the journal internals. Extremely powerful and dangerous - use with caution.",
    "template": "(*eval* <admin-secret> <expression>)",
    "example": "(*eval* \"admin-secret\" (+ 1 2 3))",
    "permission": "root"
  },
  "*call*": {
    "description": "Calls a function with the root object as an argument. The function receives direct access to the root data structure.",
    "template": "(*call* <admin-secret> <function>)",
    "example": "(*call* \"admin-secret\" (lambda (root) ((root 'get) '(control object ledger))))",
    "permission": "root"
  },
  "*step*": {
    "description": "Executes a full step cycle: refreshes configured bridges, commits the current staged state, and returns the new chain size.",
    "template": "(*step* <admin-secret>)",
    "example": "(*step* \"admin-secret\")",
    "permission": "root"
  },
  "*set-secret*": {
    "description": "Changes the admin secret used for root-level operations. Requires the current admin secret for authentication.",
    "template": "(*set-secret* <current-secret> <new-secret>)",
    "example": "(*set-secret* \"old-secret\" \"new-secret\")",
    "permission": "root"
  },
  "*set-step*": {
    "description": "Sets the step handler function that is called during *step* operations. The function receives the root object and admin secret.",
    "template": "(*set-step* <admin-secret> <step-function>)",
    "example": "(*set-step* \"admin-secret\" (lambda (root secret) ...)",
    "permission": "root"
  },
  "*set-query*": {
    "description": "Sets the query handler function that processes all incoming queries. The function receives the root object and query data.",
    "template": "(*set-query* <admin-secret> <query-function>)",
    "example": "(*set-query* \"admin-secret\" (lambda (root query) ...)",
    "permission": "root"
  }
}
