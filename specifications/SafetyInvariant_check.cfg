\* check only safety invariants
\* java -cp tla2tools.jar tlc2.TLC -config SafetyInvariant_check.cfg SyncWebJournal.tla

SPECIFICATION Spec

CONSTANTS
    Values    = {"v1", "v2", ""}
    MaxWindow = 1
    Paths     = {p1, p2}
    MaxIndex  = 3

SYMMETRY Symmetry \* for paths

CONSTRAINT StateConstraint

INVARIANTS
    TypeInvariant
    WindowConstraints
    stepIndexInvariant
    SafetyInvariant
