networks:
  ledger-net:
    name: ${COMPOSE_PROJECT_NAME:-ledger}-net

services:
  journal:
    image: ghcr.io/sandialabs/sync-web/journal-sdk:${SYNC_WEB_VERSION:-1.5.0}
    container_name: ${COMPOSE_PROJECT_NAME:-ledger}-journal
    working_dir: /srv
    entrypoint: ["/bin/sh", "/srv/run.sh"]
    networks:
      - ledger-net
    environment:
      SECRET: ${SECRET:-}
      INTERFACE_SECRET: ${INTERFACE_SECRET:-}
      SYNC_WEB_VERSION: ${SYNC_WEB_VERSION:-1.5.0}
      JOURNAL_PORT: ${JOURNAL_PORT:-8192}
      PERIOD: ${PERIOD:-2}
      WINDOW: ${WINDOW:-1024}
      RUST_LOG: ${RUST_LOG:-INFO}
      JOURNAL_UPDATE: ${JOURNAL_UPDATE:-}
      INTERFACE_ADMINS: ${INTERFACE_ADMINS:-admin}
      INTERFACE: ${INTERFACE:-}
      JOURNAL_NAME: ${JOURNAL_NAME:-}
      LISP_DIR: /srv/lisp
    volumes:
      - database:/srv/database
      - ../../../records/lisp:/srv/lisp:ro,Z
      - ./run.sh:/srv/run.sh:ro,Z
    ports:
      - "${JOURNAL_PORT:-8192}:80"
    healthcheck:
      test: ["CMD-SHELL", "wget -q -O- http://127.0.0.1/ >/dev/null"]
      interval: 10s
      timeout: 5s
      retries: 6
      start_period: 20s

volumes:
  database:
    name: ${COMPOSE_PROJECT_NAME:-ledger}-database
